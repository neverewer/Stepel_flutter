import 'package:stepel_flutter/imports.dart';

class Settings {
  Settings._();

  static const Duration welcomePageCardListAnamationDuration = Duration(milliseconds: 300);
  static const Curve welcomePageCardListCurve = Curves.linear;
}
